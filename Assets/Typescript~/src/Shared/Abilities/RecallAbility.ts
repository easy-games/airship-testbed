import { AbilityLogic } from "@Easy/Core/Shared/Abilities/AbilityLogic";

export default class RecallAbility extends AbilityLogic {
	// public override OnEnabled(): void {
	// 	print("Enabled recall ability for entity", this.entity.GetDisplayName());
	// }
	// public override OnDisabled(): void {
	// 	print("Disaabled recall ability for entity", this.entity.GetDisplayName());
	// }
	// public override OnActionPressed(): void {}
	// public override OnActionReleased(): void {
	// 	print("now we invoke some AbilityAction w/ cancellable stuff");
	// }
}
