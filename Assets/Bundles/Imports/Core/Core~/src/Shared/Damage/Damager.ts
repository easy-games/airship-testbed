import { DamageType } from "./DamageType";
/*
declare interface DamagerData {
	damageType: DamageType;
	onHitVFX: string;
	onHitSoundEffect: string;
}
class Damager {
	constructor() {
		ClientSignals.EntityDamage.ConnectWithPriority(SignalPriority.MONITOR, (event) => {
			if (event.damageType === damageType) {
				SoundManager.PostEvent(onHitSoundEffect, event.entity.Model);
			}
		});
	}
}*/
