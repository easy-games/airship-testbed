export declare class CoreUI {
    /**
     * Adds UI sounds.
     * @param gameObject
     */
    static SetupButton(gameObject: GameObject, config?: {
        noHoverSound?: boolean;
    }): void;
}
